# 📘 **DEV_WORKFLOW.md — правила взаимодействия нейронок**

```md
# DEV_WORKFLOW.md

1. Агент A (backend) всегда:
   - читает ARCHITECTURE.md
   - использует API_CONTRACT.md как источник истины
   - не пишет frontend
   - обновляет openapi.json
   - делает миграции Alembic
   - обновляет docker-compose при добавлении сервисов

2. Агент B (frontend) всегда:
   - читает API_CONTRACT.md
   - генерирует TS клиент
   - не пишет backend
   - не меняет API форматы

3. Любая фича = такой процесс:
   1) Backend обновляет API_CONTRACT.md  
   2) Backend реализует API  
   3) Frontend генерирует клиент  
   4) Frontend реализует UI  

4. После каждого спринта:
```

make down
make up

```
Ты вручную проверяешь функционал.

5. Каждый агент пишет код строго в своей зоне ответственности.

6. Никаких непредусмотренных библиотек/архитектур/паттернов.
```

---

