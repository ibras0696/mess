# 🗒️ FRONT.md — журнал прогресса frontend (Агент B)

Использование: каждый фронтенд-апдейт фиксируем отдельным блоком. Не удаляй историю, новые записи добавляй сверху.

## Правила ведения
- Следуем API_CONTRACT.md, не придумываем API.
- TS HTTP-клиент генерим из OpenAPI; руками не правим `api/generated/`.
- WebSocket клиент следует событиям из API_CONTRACT.md.

## Шаблон записи (копируй и заполняй)
```
## YYYY-MM-DD — Спринт N (кратко о фокусе)
- Сделано:
  - ...
- В работе:
  - ...
- Блокеры/риски:
  - ...
- Следующие шаги:
  - ...
- Артефакты:
  - PR: <ссылка или "-" >
  - OpenAPI TS-клиент: сгенерирован? (да/нет)
  - Дизайн/решения UI: <коротко/нет>
```

## Журнал
*(ниже добавляй записи по шаблону — свежие выше)*

## 2025-12-03 — Спринт 5 (файлы + rate-limit)
- Сделано:
  - Добавлен presign upload: компонент FileUploader вызывает POST /attachments/presign и грузит файл PUT-ом, прикрепляет metadata к сообщению.
  - Отправка сообщений по WS теперь включает attachments; REST-фоллбек уведомляет, что вложения требуют WS.
  - UI показывает прикреплённые файлы в сообщениях; обработка 429 (rate limit) для REST отправки.
- В работе:
  - Получение/отображение attachments из REST/WS после того как бэк начнёт отдавать их в MessageRead.
- Блокеры/риски:
  - OpenAPI MessageRead пока без attachments; WS payload с attachments обрабатывается заранее, но бэк их не сохраняет.
- Следующие шаги:
  - Дождаться бэка с attachments в Message, перегенерить клиент; подключить delivered/read/online при появлении.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: обновлён
  - Дизайн/решения UI: FileUploader, индикаторы вложений

## 2025-12-03 — Спринт 4 (WebSocket)
- Сделано:
  - Добавлен WS клиент с автопереподключением, обработка событий message_sent/new_message/typing.
  - Отправка сообщений теперь через WS (с temp_id) с REST fallback; история подтягивается REST, новые приходят live.
  - Индикация typing в чатах, protected маршруты сохраняются.
  - docker-compose/backend порт теперь настраивается через BACKEND_PORT (по умолчанию 8000); перегенерен OpenAPI клиент после обновления бэка (attachments/ws).
- В работе:
  - delivered/read/online события и улучшение UX (debounce typing).
- Блокеры/риски:
  - WS контракт без delivered/read пока не используется.
- Следующие шаги:
  - Добавить статусы доставки/прочтения, reconnection UI, кеш/пагинацию истории.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: актуален
  - Дизайн/решения UI: WS-интеграция с индикатором typing

## 2025-12-03 — Спринт 3 (REST чаты)
- Сделано:
  - Сгенерирован TS OpenAPI клиент (auth, users, chats, messages) из свежего openapi.json.
  - Подключены чаты: список и создание POST /api/chats с формой, переход в комнату.
  - Подключена история сообщений /api/chats/:id/messages + отправка send_message (REST).
  - Bootstrap auth теперь использует refresh + /users/me через сгенерированные клиенты; защищённые маршруты активны.
- В работе:
  - WS реал-тайм и typing/delivered (следующий спринт).
- Блокеры/риски:
  - Нет: бэкенд auth/chats в main, openapi.json получаем при поднятом backend.
- Следующие шаги:
  - Добавить WS клиент и live обновления сообщений/статусов.
  - Кэш/пагинация истории, UX улучшения.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: сгенерирован
  - Дизайн/решения UI: формы чата/сообщений, protected routing

## 2025-12-03 — Спринт 2 (sync с backend auth)
- Сделано:
  - Подтянул изменения бэкенда (JWT auth). Фронт адаптирован к новым ответам TokenPair: токены + user с created_at, token_type=bearer.
  - Регистрация теперь сразу логинит и сохраняет токены/пользователя в Zustand/localStorage.
  - Добавлен bootstrap/refresh: при старте пробуем refresh + /users/me, защищённые маршруты (chats/chat/:id) через ProtectedRoute.
- В работе:
  - Генерация TS OpenAPI клиента и подключение чатов/сообщений.
- Блокеры/риски:
  - Нет openapi.json, проверка auth e2e возможна только после запуска бэка с миграциями.
- Следующие шаги:
  - Интегрировать /users/me и refresh в автологин, связать список чатов и чат-ленты с API.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: нет
  - Дизайн/решения UI: auth формы с обработкой ошибок от API

## 2025-12-03 — Спринт 2 (Auth + роутинг)
- Сделано:
  - Добавлен роутинг (react-router-dom), общий layout и страницы home/login/register/chats/chat/:id.
  - Реализован auth flow по контракту: формы login/register, HTTP-клиент на fetch, токены сохраняются в Zustand + localStorage.
  - Добавлены ENV конфиги (VITE_API_URL/WS_URL), обновлены доки, подготовлены заглушки REST/WS.
- В работе:
  - Генерация TS OpenAPI клиента и интеграция /chats и /messages.
- Блокеры/риски:
  - Нет openapi.json и реализованных auth эндпоинтов на бэкенде для e2e проверки.
- Следующие шаги:
  - Сгенерировать TS-клиент, подключить чаты/сообщения и refresh/me.
  - Добавить guard на защищённые маршруты, подключить WS typing/delivered.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: нет
  - Дизайн/решения UI: навигация + auth формы

## 2025-12-03 — Спринт 1 (скелет фронта)
- Сделано:
  - Скаффолден Vite + React + TS, добавлен Tailwind 3 и базовый UI-превью мессенджера.
  - Заложена структура по FRONTEND_STRUCTURE (api/ws/http/generated, store, pages, components, hooks, utils, styles).
  - Dockerfile и docker-compose обновлены для `npm run dev -- --host`; node_modules собраны через npm ci.
- В работе:
  - Нет (готов перейти к auth/chats после появления OpenAPI клиента).
- Блокеры/риски:
  - OpenAPI json/клиент ещё не сгенерен, поэтому HTTP-логика — заглушки.
- Следующие шаги:
  - Сгенерировать TS-клиент по openapi.json, подключить auth flow (login/register).
  - Подключить Zustand store к mock данным, добавить роутинг страниц.
  - Начать интеграцию REST чатов (GET/POST /chats) и истории сообщений.
- Артефакты:
  - PR: -
  - OpenAPI TS-клиент: нет
  - Дизайн/решения UI: базовый landing с предпросмотром чатов/сообщений
